import { SelectboxDirective } from './selectbox.directive';

describe('SelectboxDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectboxDirective();
    expect(directive).toBeTruthy();
  });
});
